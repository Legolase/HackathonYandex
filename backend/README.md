# Backend for kilogram messenger

### Install dependencies:
`npm install`
### Start migration:
`npm run migrate`
### Start server:
`npm start`



### Create new migration:
`npm run create-migration NAME OF MIGRATION`
